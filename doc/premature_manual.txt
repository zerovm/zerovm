this document is under construction, don't use it!

описание zerovm. что требует zerovm для работы, что гарантирует.

zerovm - это минимальная виртуальная машина для запуска nacl nexe modules
zerovm не поддерживает большую часть nacl syscalls, вместо этого он использует механизм 
  передачи управления в изолируемый код (syscallback) и позволяет ему сделать всю работу.
  при этом untrusted код может использовать zerovm api для получения обслуживания 
  ввода/вывода, выделения памяти, завершения работы и т.д.

создание приложения для выполнения под управлением zerovm
  nacl sdk
  zerovm api
  zrt library

описание механизма syscallback
  (большую часть текста можно взять из "syscallback.txt")
  для чего нужен syscallback
  принципы работы
  
описание zerovm api

описание каналов
  типы каналов (всего 5)
  функции api для работы с каналами
  ошибки работы с каналами
  использование каналов с zrt
  локальные каналы
  сетевые каналы
  специальные каналы

описание библиотеки zrt

описание манифеста. допустимые ключи
  (часть текста можно взять из "manifest.txt")
  Channel -- используется для описания канала. реализует файловую абстракцию предлагаемую zerovm
  CommandLine -- служит для передачи комнадной строки в пользовательскую программу
  Environment -- служит для 
  Version -- 
  Nexe --
  NexeMax -- 
  SyscallsMax --
  NodeName --
  NameServer --  
  
поведение zerovm в случае возникновения неисправимой ошибки (signal) в (un)trusted коде

установка zerovm, nacl sdk e.t.c.